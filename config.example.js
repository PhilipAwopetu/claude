// BetLens local config — copy this file to config.js and fill in your key.
// config.js is gitignored: NEVER commit your real API key to the repository.
window.BETLENS_CONFIG = {
  apiKey: "sk-ant-PASTE-YOUR-KEY-HERE"
};
